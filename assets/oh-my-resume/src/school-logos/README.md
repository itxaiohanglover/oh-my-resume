# Built-in school logos

These assets are used by the resume header school-logo picker. Keep the
manifest key stable because Markdown files may reference it directly.

The initial images were supplied by the project owner. `logo1.png` from the
source set was excluded because its visible pixels did not contain a usable
school mark.
